# Files whose name does not carry a content hash, including the HTML shell.
# "expires -1" emits Cache-Control: no-cache, so the client revalidates on
# every request and a redeploy becomes visible immediately.
expires -1;
