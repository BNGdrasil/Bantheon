# Content-hashed build output only. The file name changes on every
# redeploy, so the response may be cached for a year without revalidation.
expires 1y;
add_header Cache-Control "public, immutable";
